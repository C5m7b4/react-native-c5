import { BasicChart } from './charts';
import { BasicChartProps, BasicChartState } from './types';
import { quickSort, newShade } from './utils';

export const multiply = (a: number, b: number) => a * b;

export { BasicChart, BasicChartProps, BasicChartState };
export { quickSort, newShade };
